# Later — decision-model seams

**Status:** Parked. Seams reserved; no vendor lock in this note.

Closed moments where a typed decision model may later replace a language-model call:

- emit / withhold / abstain
- which capped action to recommend (including “no action”)
- whether a named secondary domain applies
- whether a cross-section conflict is present
- which autonomy class label applies (or human-only)
- which configured owner role to propose

Do not wire a specific product until the L4 agentic system shell is solid and evals demand it.
